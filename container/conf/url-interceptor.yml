public:
  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: ''
     resourceId: home
     exact: true
     scopes:
     - read
  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: WEB-INF
     exact: false
     scopes:
     - read
  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: /static
     resourceId: static_contents
     exact: false
     scopes:
     - read

authenticated:
  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: /api/buyers
     resourceId: buyer
     scopes:
     - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: /api/buyers/\d+
     resourceId: buyer
     scopes:
     - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/activities
    resourceId: activity
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/activities/\d+
    resourceId: activity
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/users
    resourceId: user
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/users/\d+
    resourceId: user
    scopes:
      - read


  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/delayReasons
    resourceId: delay_reason
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/delayReasons/\d+
    resourceId: delay_reason
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/departments
    resourceId: department
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/departments/\d+
    resourceId: department
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/garmentTypes
    resourceId: garment_type
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/garmentTypes/\d+
    resourceId: garment_type
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders
    resourceId: order
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders/\d+
    resourceId: order
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders/\d+/logs
    resourceId: order
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders/\d+/activities/\d+/logs
    resourceId: order
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/seasons
    resourceId: season
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/seasons/\d+
    resourceId: season
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/teams
    resourceId: team
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/teams/\d+
    resourceId: team
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/timelines
    resourceId: timeline
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/timelines/\d+
    resourceId: timeline
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/calendar
    resourceId: calendar
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/settings
    resourceId: setting
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/settings/[a-zA-Z_0-9-]+
    resourceId: setting
    scopes:
      - read

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/templates
    resourceId: template
    scopes:
      - read


protected:
  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: /api/buyers
     resourceId: buyer

  - !com.jazasoft.mtdb.dto.UrlInterceptor
     url: /api/buyers/\d+
     resourceId: buyer

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/activities
    resourceId: activity

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/activities/\d+
    resourceId: activity

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/delayReasons
    resourceId: delay_reason

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/delayReasons/\d+
    resourceId: delay_reason

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/departments
    resourceId: department

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/departments/\d+
    resourceId: department

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/garmentTypes
    resourceId: garment_type

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/garmentTypes/\d+
    resourceId: garment_type

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders
    resourceId: order

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/orders/\d+
    resourceId: order

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/seasons
    resourceId: season

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/seasons/\d+
    resourceId: season

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/teams
    resourceId: team

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/teams/\d+
    resourceId: team

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/timelines
    resourceId: timeline

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/timelines/\d+
    resourceId: timeline

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/users
    resourceId: user

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/users/\d+
    resourceId: user

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/settings
    resourceId: setting

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/settings/[a-zA-Z_0-9-]+
    resourceId: setting

  - !com.jazasoft.mtdb.dto.UrlInterceptor
    url: /api/templates
    resourceId: template


